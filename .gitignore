# Segredos / ambiente
.env
.env.*
!.env.example
chaves_secretas.txt

# Config local de sessão (scratch)
**/settings.local.json

# SO / logs
*.log
.DS_Store
node_modules/

# Índice local (regenerável) — o MAPA e o CATÁLOGO em .md SÃO versionados
memory/claude_index.db
memory/claude_index.db.bak

# Banco de conversa do agentbridge — estado local de sessão
.agentbridge/

.forge/

# lockfiles do plugin Claude Code na IDE — contêm authToken, NUNCA versionar
ide/
# Config local .codex/ — contém URL MCP, não versionar
.codex/
