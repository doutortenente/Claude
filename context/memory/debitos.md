# DÉBITOS / RISCOS — detalhe completo
<!-- NÃO carrega sozinho. Abrir quando for mexer no SASI ou quando ele perguntar o que está pendente. -->
<!-- Índice curto dos ABERTOS vive em comando.md. Aqui fica o detalhe + o histórico do que foi resolvido. -->

## ABERTOS

### SASI — dados e schema
- **`evolucoes` JSONB, 2 schemas convivendo**: ingest usa `pa_sys_max`/`cr1`, ficha grava `pas1`. Hoje só o ingest tem dado real. Inclui inversão de convenção da PAM (ingest `pam1`=MIN/`pam2`=MAX × ficha `pam1`=MAX/`pam2`=MIN). Correção de tipo confirmada via Supabase MCP 01-jul: `impressao` e `conduta` são `text[]` (array de texto Postgres), NÃO jsonb — só `problemas_ativos`/`condutas_sistemas` e os snapshots por sistema são jsonb de fato. Referência errada em `sasi/CLAUDE.md` ("evolucoes JSONB") pendente de correção. **Unificação (P3, 02-jul) NÃO finalizada** — operador ainda sem padrão fixo; proposta = vocabulário canônico `sistema_parametro_max/min` + labs indexados por DATA (não por posição) + prefixo por eixo clínico. Decisões clínicas já cravadas por ele: Leuco + Tax + PCR → eixo **Infecto**; Lactato → **Hemodinâmica**; INR/coagulação → **Hepato/TGI**.
- **Pareamento problema⇄conduta é POSICIONAL** (04/05-jul, mesma família): a doutrina Ramo C 1:1 depende dos arrays `impressao`/`conduta` terem mesmo tamanho e ordem, sem checagem no frontend. No plantão real os dados foram corrigidos à mão. Se um array vier maior/menor, o pareamento quebra em silêncio.
- **Correção de evento não propaga pro TEXTO da evolução** (achado 01-jul, Luiz Jose UTI2-L4, `evolucao_id 4922e263-db7d-4c88-941c-b654159c8982`): Hb 4,1 (erro de OCR) foi deletado dos `eventos_clinicos` em 30-jun mas ficou intacto na `impressao` até correção manual em 01-jul (valor real Hb 14, informado pelo operador). Limpar evento ≠ limpar prosa; nada sincroniza os dois.
- **`source_text` sem o valor**: registros de 27-jun guardaram só o cabeçalho ("gasometria STAT"). Conserto à montante, na skill `sasi-ingest-export` (incluir o valor, não só o cabeçalho).
- **`atbs`/`culturas` = 0 em produção** — stewardship incompleto.
- **`SASI_OPERATOR_USER_ID` ausente** (16-jul): consumida por `sasi/mcp-server/src/tools/ingest-deploy.ts:57,93` mas NUNCA existiu no `.env` — débito PRÉ-EXISTENTE, conferido contra o `.env` antigo, não é regressão da reescrita. Efeito: `user_id` do `ingest_audit_log` grava `null` em todo ingest. Fix: operador decidir o UUID correto (uso solo com `dev_bypass` pode tornar irrelevante — decisão dele).
- **`eventos_clinicos`**: 135 linhas. Fila de revisão ZERADA em 30-jun (validados K2,6 / leuco 3500 / plaq 28k de 24-jun + Na 161 de 27-jun; deletados 2 erros de extração da gaso STAT de 27-jun — glicemia 15 mg/dL [provável mmol/L ≈270] e Hb 4,1). Script: `~/projetos/scripts/sasi/audit_eventos.py`.

### SASI — alertas e clínica
- **7 regras de alerta eng-default sem `fonte`** (glicemia/K/Na/temp) — aguardando validação Vera Health com DOI. **Além disso faltam regras inteiras** (achado no plantão real 04/05-jul): hipercalcemia (CaI 1,63 — Gisleina/L12), hipocalemia (K 3,2 — Luiz/L04) e acidose (pH 7,29 — Sonia/L01) foram gravadas SEM disparar alerta porque não existe regra em `alert_rules` pra esses 3 casos. Não é bug de execução, é cobertura incompleta. Criar as 3 com fonte/DOI fora do plantão.
- **Oligúria**: o gatilho depende do tipo `diurese` no ingest (`bh_h` ≠ diurese); a skill `sasi-ingest-export` precisa capturar o campo.
- **SOFA**: ruleset congelado `SOFA1_v1.0_2026-06-30` em `sasi/docs/SOFA-RULESET.md` (cutoffs Vincent 1996 / Sepsis-3 Singer 2016, sem ancoragem DOI no acervo Vera ainda); view `vw_sofa_diario` v0.2. PENDENTE: (a) operador validar os cutoffs antes de uso clínico à beira-leito; (b) dados escassos — 16 registros antigos incompletos, sem retroação. Motor SOFA v1 = FASE B (imputação determinística + LOCF + audit trail + `sofa_janela` + ΔSOFA≥2), travado por VOLUME de dados, não por cutoff. SOFA manual e automático coexistem por ora; débito menor: `sofaColorClass` duplicada em 2 lugares.
- **Ordenador de prescrição por sistema** — modo D da skill `sasi-ingest-export` (`references/07-export-prescricao-ordenada.md`): 7 blocos canônicos + safety check CONSERVADOR (só contraindicação rastreável, nunca inventa interação). Fluxo faseado: texto revisável agora, SQL é FASE 2. Parciais abertas: `patient_summary` vazia, lab serial visual, refs normais por exame.
- **Backlog clínico** em `sasi/docs/BACKLOG-CLINICO.md`. Planilha-mãe legado: `sasi/docs/legado/planilha-uti-estrutura.md` (AppSheet, abr-2026).

### SASI — infra e código
- **RAG protocolos**: migration `06` versionada, falta aplicar no Supabase + Edge `protocolo-ingest`. CONFIRMADO via banco: tabelas `protocolos`/`protocolo_chunks` e RPC `match_protocolos` NÃO existem em produção (a migration está em `_archive/`, nunca foi aplicada). Notas em `sasi/memory/notes/rag-protocolos.md`. **Spec Vera Health (30-jun)**: JSON `{titulo, fonte_arquivo, trechos:[{conteudo, fonte_anchor, fonte_pagina, fonte_secao}]}` — texto + âncora de fonte, SEM vetor (a Edge gera o embedding com gte-small). Distinto do `VERA-INGEST-SPEC.md`, que cobre `alert_rules`, não protocolos.
- **Check-up de segurança 03-jul não endereçado**: 11 `dev_bypass`, extensões `pg_trgm`/`vector` no schema `public`, leaked-password-protection off, `memorias` sem policy, 180 `multiple_permissive_policies`, 10 índices sem uso.
- **`gaso-atomica`**: depende de liberar a constraint `eventos_clinicos_tipo_check`, que bloqueia tipos novos.
- **`dispositivos`**: upgrade de estado+data requer coordenação com o frontend (o shape booleano atual ainda é lido pelo dashboard).
- **INTERCONSULTAS (View 2 / Paciente Sumário) não implementada** — tabela `interconsultas` não existe no schema. Spec completa em `~/Downloads/Files_to_claudecode/design_handoff_comando_uti/CORRECTION.md` (especialidade/status/nota + programação/pendências, com RLS). Pendente também mover `CORRECTION.md` + `IMPLANT.md` pra `sasi/docs/`.
- **Ficha de Evolução redundante** (achado do operador 02-jul): "20 formas de fazer a conduta"; os campos deveriam puxar direto do texto já extraído em vez de exigir preenchimento paralelo. NÃO iniciado.
- **Lint**: 1 erro pré-existente `frontend/src/lib/exportPDF.ts:63` `no-control-regex` (regex com `\x00` proposital da sanitização de PDF; falha idêntica em Node 20 e 24) + 13 warnings de estilo (`no-explicit-any`, `exhaustive-deps`, `only-export-components`). Não bloqueia build/deploy; limpar quando tocar no arquivo.
- **Edge Function `health`** (SDK oficial `@supabase/server`, código em `sasi/supabase/functions/health/`, commit `206393d`): está só no repo, SEM CONSUMIDOR — não foi deployada e nada no app a chama. Mesmo princípio do fluxo faseado. Pendente decidir se/quando deployar e quem consome.
- **CI ampliado** (mcp-server + clinical-engine): DESTRAVÁVEL desde 29-jul-2026 — o `GITHUB_CLASSIC_PAT` novo (renovado 29-jul, `.env` §3) tem scope total INCLUINDO `workflow`, ativo no `gh` e no `~/.git-credentials`. Falta só EXECUTAR o push do workflow (nada mais bloqueia).
- ~~**`pnpm check` FALHA por `SystemPanel.tsx`**~~ (11-ago) — **MORTO em 14-ago-2026**: o arquivo nunca chegou a entrar no repositório. Ele vive no `stash@{0}` (`pre-pull-14ago-rascunhos-locais`), junto de `ProblemRow.tsx`, `VitalStat.tsx` e `src/proxy.ts`, e nenhum deles é importado por ninguém. Medido do caminho novo: `pnpm check` = 303 testes em 18 arquivos, exit 0. **O débito não foi consertado, foi engavetado** — se o operador desengavetar o stash, os 5 erros de tipo voltam com ele.
- ~~**Working tree com 56 arquivos sujos divergindo da main**~~ (11-ago) — **MORTO em 14-ago-2026**: absorvido pelo merge `0c15203`. Working tree limpo e `main` em dia com o GitHub.
- **Dois stashes guardados no SASI-V3, decisão do OPERADOR** (14-ago-2026): `stash@{0}` `pre-pull-14ago-rascunhos-locais` — `src/proxy.ts` (renovação de sessão do Supabase; o app hoje não tem nem `middleware.ts` nem `proxy.ts`, mas login está riscado do escopo) + os 3 componentes órfãos acima. `stash@{1}` `colisoes-ff-design-system` (08-ago) — 472 linhas, 430 delas no `globals.css`, **anteriores à reconstrução de 09-ago**, provavelmente mortas. Stash não expira, mas some se alguém rodar `git stash clear`.
- **Smoke plantão E2E**: checklist aberto em `sasi/docs/STATUS.md` §6.
- **Apagão 18-jul-2026**: dados clínicos deletados em massa, autor desconhecido. Banco repovoado no plantão. **Sem backup no plano atual** — risco vivo.

### Fora do SASI
- 🔴 **CELEBRO PÚBLICO NO GITHUB COM 7 ARQUIVOS DE PHI VERSIONADOS** (achado 11-ago-2026): a pasta real de dados de paciente chama `PHI-LOCAL/`, mas o `.gitignore` protege `90-PHI-LOCAL/` — nomes diferentes, filtro nunca pegou, 7 arquivos subiram pro repositório público. **AÇÃO É DO OPERADOR** (só ele mexe na visibilidade do repo): (1) tornar o repo PRIVADO no GitHub; (2) `git rm --cached` nos 7 arquivos + commit; (3) corrigir o `.gitignore` pro nome real da pasta. Histórico do git continua com o conteúdo mesmo após o `rm --cached` — reescrever histórico é decisão dele.
- **MCP `supabase` com "Invalid ID"** (03-jul): servidor remoto SAUDÁVEL (sonda direta respondeu 401 padrão com fluxo OAuth válido e aceitou o `project_ref`); o defeito é cadastro OAuth velho guardado no lado local (`~/.claude/.credentials.json`). **Fix só pelo operador**: `/mcp` → supabase → **Clear authentication** → Authenticate de novo. Enquanto isso, quem funciona em modo FULL é o conector `mcp__claude_ai_Supabase`. `read_only=true` na URL confirmado 01-jul. Decisão dele: `.mcp.json` é o principal quando autenticado; NÃO duplicar a skill Supabase no repo sasi.
- **Chaves Google**: `GEMINI_API_KEY` trocada 03-jul (projeto `111104277806`) — autentica (HTTP 200 na listagem de modelos) mas a geração de conteúdo é bloqueada (403/429) pela CONTA/PROJETO Google dele, não pela chave. `VERTEX_STUDIO_KEY`/`VERTEX_AI_API_KEY` seguem HTTP 400. Mesma raiz trava qualquer Gemini via API (incl. Gemini como 2ª opinião) até ele destravar a conta ou usar outra.
- **iPad (11 Pro) sem sync em tempo real**: LiveSync morto DE VEZ (Jarvis/VPS expurgado 28-jul-2026, sem retomada); via única Remotely Save→OneDrive já instalada, falta o login Microsoft dele (só ele autentica).
- **Conflito de rótulo no `.env`** (16-jul): o valor `[SEGURO_REDACTED]` estava salvo como `SASI_LABS_LICENSE`; na reescrita o operador rotulou o mesmo valor como "Bloco de Notas Bear — API KEY". Mantido o rótulo novo `BEAR_NOTES_API_KEY`, com comentário registrando o conflito. Pendente ele arbitrar.
- **`uti-tracker`** (GitHub, app mobile React Native+Firebase) — decisão 01-jul: arquivar, não reviver (redundante com SASI, risco de dispersão AH/SD). Canibalizar 3 ideias pro backlog SASI antes: `batch-import-modal`, `sedation-widget`, `clinical-alerts-widget`.
- **Motor da passagem** (`build_passagem.py`): paralelizar a extração de números pelo LLM (ganho de velocidade). Uso real já provado em plantão (UTI2 noturno, 7 leitos, docx gerado).
- **Drive→OneDrive, resíduos**: (1) Adobe Document Cloud — 1 doc com ponteiro no `_INDEX.md` (`acrobat.adobe.com/id/urn:aaid:sc:VA6C2:4338c970-ea24-4055-9ae7-5bf59710b2b0`), pendente download manual dele; (2) `00_Duplicatas/` — 11 arquivos de MESMO NOME e CONTEÚDO DIFERENTE, preservados por hash QuickXor, a revisar; (3) ~12% da cópia que faltou (Google bloqueou o Workspace) — tarefa futura separada, sem resume automático porque os nomes já mudaram no destino.
- **RAM 8GB é teto físico** — upgrade de pente é o melhor ganho de desempenho futuro (JetBrains come ~50%).
- **`Planilha_SASI_V2.xlsx`** — usar só como FORMATO de staging revisável, NÃO virar 3º banco de dados.
- **Credenciais de produção do Supabase vazadas no chat 01-jul** (service_role JWT, senha Postgres, secret key, management token, chaves S3). Claude não usou nenhuma. **Operador decidiu NÃO rotacionar — risco aceito.** Se um dia rotacionar, basta atualizar `~/projetos/.env` (o wrapper do MCP sasi pega sozinho).
- **Escalistas** (23-jul): disponibilidade DEFINIDA e já preenchida no template (imediata — dias úteis, noites e FDS; agenda real dele hoje = 1 plantão 24h quinzenal na BP). Falta só DISPARAR o lote de 5 a 8 contatos na semana.
- **ACLS 2023 vencido** (23-jul): renovação pendente (pré-requisito frequente pra UTI/PA).
- **Exa MCP aguardando API key** (23-jul): operador quer o Exa (`exa-labs/exa-mcp-server`, busca web em tempo real pra agentes) nos dois Claudes. NÃO instalado — exige `EXA_API_KEY` que só ele cria (dashboard.exa.ai, tem tier grátis). Quando a chave chegar: gravar no `.env`, entrada `npx exa-mcp-server` em `~/projetos/.mcp.json` + `claude_desktop_config.json`, provar handshake na mesma sessão. Contexto da instalação: memória do agente `claude-desktop-tijolao.md`.
- **Obsidian `advanced-uri` desativado em observação** (23-jul): plugin desligado na faxina; se nenhum fluxo quebrar até 30-jul, remover.
- **Goggins nas preferências pessoais do claude.ai** (28-jul): colar a diretriz Goggins (`~/.claude/CLAUDE.md` §2) nas preferências pessoais do claude.ai, pra cobrir Claude chat/celular além do Claude Code. Só o operador pode (conta pessoal).
- **PRD Alpha Council → Cowork online** (28-jul): levar `~/projetos/PRD-alpha-council-war-room.md` pro Cowork online (claude.ai, plugin Produtividade), criar o projeto e disparar "Comece a construir — inicie com o Bloco 0". Só o operador pode.
- **Restart do Claude Code pendente** (28-jul): ativa o `ULTRACOST_GATE=off` já gravado em `~/.claude/settings.json`; o model pin da settings volta pro que está configurado. Só o operador pode. **29-jul**: a sessão atual também precisa de `/mcp reconnect` no `jetbrains-index`/`jetbrains` — a IDE reiniciou 2× na madrugada e a porta mudou.
- **chrome://flags Gemini Nano** (28-jul): desabilitar "optimization guide on device model" (Disabled) no Chrome — senão os 4,1G do Gemini Nano voltam a baixar. Só o operador pode.
- **MCPs órfãos do expurgo Claude Desktop** (28-jul): `basic-memory` (gravava em `~/vaults/celebro/basic-memory/`) e `grafana` morreram junto com o Claude Desktop/Cowork. Religar no Claude Code SÓ se surgir missão que exija. 29-jul: entrada morta do `basic-memory` REMOVIDA do `~/projetos/.mcp.json` (binário não existia, ENOENT no /mcp); religar = reinstalar + re-adicionar config. No mesmo dia, `jetbrains-steroid` consertado: JAVA_HOME apontava pro `~/.local/jdk-25` apagado por faxina → `~/.local/jdk-25` RESTAURADO com Temurin JRE 25.0.3 LTS standalone (62MB, sem vínculo com IDE nenhuma), handshake provado.
- **Deleções não commitadas em `claude/` e `celebro`** (achado 29-jul na sincronização de repos): `claude` tem 2 arquivos apagados do disco sem commit (`.claude/skills/humanizer` — era symlink — e `docs/mcp-steroid.md`); `celebro` tem 13 arquivos apagados do disco sem commit (`Trampo/busca-vagas.xlsx`, `raw/mensagem-escalistas-uti.md`, `conhecimento/projetos/jarvis.md` + `runbook-jarvis-brain-swap.md` + `guia-jetbrains.md`, notas de sessão/Excalidraw, refs `_arquivo/` mortas). Arquivos já NÃO existem no disco (não é untracked, é deleção real órfã de commit) — GitHub ainda tem as versões antigas. Secretária reportou em vez de commitar (ordem explícita do operador na tarefa de 29-jul: "reporte antes" quando achar pendência fora do `.env`). PENDENTE: operador confirmar se foram deleções intencionais (ex.: faxina anterior) pra secretária commitar a deleção, ou se algo precisa ser restaurado do `git checkout`.

## RESOLVIDOS — registro, não ação
- **29-jul (manhã)** Ordem "Re mate, remova" executada: (1) Settings Sync re-morto em dobro (`com.intellij.settingsSync` na disabled_plugins.txt + sync off) e estrago colateral dele revertido (grazi/restClient/swagger re-desabilitados, GitToolBox religado); (2) typescript-language-server REMOVIDO do LSP4IJ (2 definições apagadas dos XMLs + `~/.lsp4ij` 26MB pra Lixeira). Achado-chave: `~/projetos/.idea/externalDependencies.xml` declarava o `ru.adelf.idea.dotenv` (plugin do crash de 30-jun) como OBRIGATÓRIO do projeto — `CheckRequiredPluginsWorker` re-habilitava ele a cada boot, essa era a máquina de ressurreição, não só o Settings Sync. Declaração esvaziada + plugin descarregado ao vivo via `PluginEnabler.disable()` (steroid), provado `carregado=false`.
- **12-jul** Migração Netlify→Vercel: Netlify bloqueado por crédito desde 05-jul → projeto importado na Vercel (root `frontend`, Vite, Hobby, commit `a3b30bd`), 4 env vars, deploy validado ao vivo com dados reais. Produção final `sasi-uti.vercel.app`; Netlify desligado (`stop_builds=true`). Residual sem ação: `AI_GATEWAY_API_KEY` no `.env` sem uso; CLI Vercel instalado sem login; `VITE_XAI_API_KEY` em uso real no frontend.
- **19-jul** Zips-fonte pós-assimilação apagados de `~/Downloads` (~223MB) por ordem dele. Conteúdo útil já vive em `claude/skills`; originais são repos públicos, recuperáveis.
- **18-jul** Catálogo das 359 skills dos 13 zips concluído (workflow `wf_2e296ce1-be9`, 37 agentes, 0 erro): 90 assimiladas (`0f6ef26`), 9 removidas por órfã/perigosa (`e671d9a`), 257 descartadas com justificativa.
- **11-jul** Skill `admissao-uti` unificada (base Ramo C + 4 enxertos da v1); commits claude `5837030` + celebro `cc277ce`.
- **11-jul** Ficha exibe o SOFA automático da view.
- **06-jul** Main do sasi tinha a iteração ERRADA do adapter da ficha (PR#4 antigo mesclado por sessão web); refinamento `d0c06c3` mesclado limpo, gate aprovado, merge `017a79c`.
- **04/05-jul** `vw_dashboard_uti` era `SECURITY DEFINER` (advisor ERROR) — endurecida em prod (`20260703220905`): `security_invoker=true`, 12 policies com `(select auth.uid())`, 9 índices de FK; commit `7b60764`.
- **04/05-jul** `pendencias`: mecanismo próprio implementado e provado em plantão real ponta a ponta (ingest→pendência→alerta).
- **03-jul** MCP `sasi` consertado — defeito duplo (env não carregado + Node <22 sem WebSocket nativo) resolvido pelo `mcp_sasi_wrapper.sh`; `sasi_sitrep` provado com dados reais.
- **03-jul** Reorg dos docs pelo mapa das 6 camadas (`0e7506e`, 66 arquivos removidos); PDFs `motor-clinico-v2` → OneDrive (`2733a42`).
- **03-jul** MCP de IDE renomeado pra `jetbrains`+`jetbrains-index`; `fix_ide_mcp.py` v2 poda por PADRÃO de nome. `.claude/settings.json` do sasi versionado (`2abd98f`).
- **02-jul** Limpeza CSS `/N` sobre `var()` (`e28faf1`): raiz real do "campo branco" era `<select>` nativo sem tema escuro.
- **02-jul** Vault celebro virou cérebro agente-first (Fases 0-7).
- **30-jun** Branches mortas `design-bayes-v2`+`resgate-design-bayes` deletadas; artefatos não-commitados resolvidos (`c3f4895`).
- **26-jun** Schema drift fechado — baseline-squash `9228069`; migrations 01-07 arquivadas; tipos TS regenerados.
- **26-jun** `alerts_log` vazia corrigida — `fn_alert_hash` (search_path) + criação de `alert_rules` (`2d5faaf`).
- **24-jun** Ficha↔Supabase síntese (migration 07, `779741a`); MCP `sasi_deploy_ingest` + clinical-engine 7 testes Vitest; template evolução D2+v2 (`35df460`).
- **03-jul** Migração pessoal Google Drive→OneDrive fechada: ~88% copiados (809 arquivos, PHI incluído por autorização consciente); 16 categorias; catálogo `_INDEX.md`+`_index.json` regenerado (803 arquivos, 114 pacientes, 2001-2026). Fora de escopo por decisão: Gmail (~33GB) e Google Fotos.
