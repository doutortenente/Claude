# LOG — histórico de sessões
<!-- NÃO carrega sozinho. Abrir quando ele perguntar "o que eu fiz" ou quando a secretária for registrar. -->
<!-- 1 linha por item. Acrescentar no fim. O que virou regra permanente sobe pro comando.md. -->

- 2026-06-20: lidos os 8 repos do GitHub; criados o CLAUDE.md global + esta memória.
- 2026-06-23: memória consolidada; auto-memory migrada pra `~/dev/`; PERFIL&CONDUTA promovido; `~/WebstormProjects` deletado.
- 2026-06-23: auditoria Supabase — índices `idx_eventos_pac_tipo_ts`/`idx_antibiograma_cultura` + `pacientes.imc`; 6 redundantes revertidos; `patient_summary`/`sofa_baseline` vazios.
- 2026-06-24: criados 5 subagentes globais (deploy-sentinel, clinical-data-auditor, code-explainer, secretaria, pubmed-evidence-checker); `claude/agents` `6fa625a`; `~/.claude/agents` virou symlink.
- 2026-06-24: `celebro` commitado e pushado (`ef66933`) — plugins, índices P.A.R.A.; PHI fora do git.
- 2026-06-24: `sasi/memory/` criada (`MEMORY.md`); índice do repo (SQLite, 244 arquivos) + limpeza; `495dc71`.
- 2026-06-24: migration `fix_functions_review` — `fn_alert_hash` corrigido (explica `alerts_log` vazia); `match_memorias`/`sync_severidade_visual` com search_path fixado.
- 2026-06-24: `sasi/plantao_queries.sql` criado (11 queries); granularidade = plantão (`ts::date`).
- 2026-06-24: Ficha↔Supabase, MCP `sasi_deploy_ingest`, migration 07, clinical-engine 7 testes, `audit_eventos.py`; main `779741a`. Template de evolução D2+v2 no repo claude (`35df460`). Handoff `sasi/docs/SECRETARIA-2026-06-24.md`.
- 2026-06-24 (tarde): 1ª tentativa do design BAYES.OPS revertida (`8eeea0b`); restaurado IBM Plex + 2 temas base.
- 2026-06-24 (tarde): rclone instalado, OAuth do Google Drive (remote `gdrive`), MCP `google-drive` restaurado.
- 2026-06-26: baseline-squash do schema de prod (`9228069`); alertas config-driven (`2d5faaf`, 25 regras); RPC `save_ficha` + fix do build (`fe36981`).
- 2026-06-26: higiene-S — `trg_autoflag_lowconf`, `vw_eventos_pendentes_revisao`, `riscos_flags`, `atbs.duracao_planejada_dias`. Módulo tendência/Δ: `trend_rules`+`fn_eval_trend`+`vw_eventos_tendencia`, 3 regras com DOI.
- 2026-06-26: repo `sasi-import` criado; docs `ROADMAP-BANCO.md`/`DECISOES-CLINICAS.md`/`VERA-INGEST-SPEC.md`.
- 2026-06-26: manutenção do PC — ~18G liberados; ZRAM ativo; ufw ativo; smartmontools/xclip/xdotool/copyq instalados.
- 2026-06-26: GUI/navegador liberado por padrão (revoga o "só eu mexo"); guardrail fica em ação destrutiva.
- 2026-06-27: design BAYES.OPS com 2 temas deployado em produção (Tactical/Clinical); main `f185ab8`. nvm default corrigido pra 20 (Netlify na época).
- 2026-06-29: Obsidian esclarecido como app desktop local; MCP `obsidian` reativado; plugin Obsidian IDE Bridge configurado. 2 skills clínicas novas (`analise-ecott`+`hemodinamica-calculada`, `4fa9c39`); jarvis `gdrive-mcp` arquivado (`55fd366`).
- 2026-06-30: higiene do PC ~3,3G; `~/.grok` deletado (1,4G); criados `pc_higiene.py`+`fix_ide_mcp.py`. Diretriz Nº0 inserida nos 5 CLAUDE.md; gavetas `_lab/`+`_arquivo/` criadas.
- 2026-06-30: operador decide NÃO trocar a senha sudo (risco aceito). Fila de revisão de `eventos_clinicos` zerada (6→0); achado o `source_text` sem valor.
- 2026-06-30: SOFA destravado — skill (`d3368e0`) + view `vw_sofa_diario`. Spec do RAG Vera Health definida; `BACKLOG-CLINICO.md`; ordenador de prescrição modo D (`866834f`). SOFA-1 ruleset congelado; view v0.2 (`3cb40de`).
- 2026-06-30: **incidente** — 3 plugins instalados na unha no WebStorm; `.env files` colidiu com o EnvY (mesma Language ID `DotEnv`) e TRAVOU o boot da IDE. Lição: antes de instalar plugin, checar se outro já registra a mesma linguagem — conflito de Language ID é crash, não aviso.
- 2026-07-01: **incidente de segurança** — operador colou credenciais de produção do Supabase no chat 2x; Claude não usou nenhuma. Feature entregue: motor determinístico da passagem `build_passagem.py` (LLM só extrai números, o script calcula Máx-Mín + balanço). Teste de fogo no L01 Valdir flagou divergência de diurese (+60mL) vs enfermagem. `e36a088`+`507efc3`.
- 2026-07-01: 3 correções de UI dos prints dele — campos brancos (`bg-app-card/60`, Tailwind `/N` não compila sobre `var()`), PDF da passagem redesenhado (`exportPDF.ts`), pendências estruturadas no MCP (`742d1f4`+`819c6c0`). Backfill de 9 pendências em 6 leitos UTI2; erro de OCR corrigido (Hb 4,1→14).
- 2026-07-02: Round 2 do "campos brancos DE VEZ" — `<select>` nativo sem tema escuro (`e28faf1`). Convenção de modelo Sonnet/Opus decidida.
- 2026-07-01/02: migração Google Drive→OneDrive iniciada; PHI incluído por autorização consciente. Google bloqueou o Workspace; aceitos ~88% (809 arquivos), 16 categorias, catálogo criado.
- 2026-07-02: vault celebro virou cérebro agente-first (Fases 0-7). Operador pediu parar de repetir aviso de PHI a cada mensagem.
- 2026-07-03: fecho da migração — `00_Revisar` (59) e `00_Duplicatas` (13) resolvidos por hash; catálogo regenerado (803 arquivos, 114 pacientes).
- 2026-07-03: MCP consolidado 12→6; wrapper do MCP sasi criado; tetos de RAM nas IDEs. Modelo default do operador virou Fable 5.
- 2026-07-03: IDEs enxugadas — DataGrip+Gateway desinstalados (~9GB); plugins de IA (`ml-llm`, `intellego`) removidos do WebStorm; PyCharm confirmado como IDE de uso diário dele.
- 2026-07-03: **Arquitetura de Informação em 6 CAMADAS travada**; decisão de NÃO vetorizar o conhecimento do celebro (wiki file-based); varredura dos repos começou pelo SASI (`0e7506e`).
- 2026-07-03: MCP `sasi` fechado de vez (Node 24 pelo wrapper); `sasi_sitrep` provado. `netlify.toml` unificado em Node 24, deploy verde byte-idêntico. Achado: rótulo mentiroso do MCP de IDE.
- 2026-07-03: earlyoom/swappiness aplicados por ele; decidido NÃO rotacionar as chaves vazadas; MCP renomeado pra `jetbrains`+`jetbrains-index`; "Invalid ID" do Supabase diagnosticado.
- 2026-07-03: skills oficiais Supabase vendoradas (`885ee1c`); `~/.agents` e `~/.cursor` apagados com OK dele.
- 2026-07-03: subagente `chefe` criado e no mesmo dia dividido em `chefe`+`caco` (`2492ee3`+`f91bed4`). `saude_pc.py` corrigido; 4 scripts v2 do catálogo resgatados.
- 2026-07-03: `residente`+`batedor` criados a pedido dele ("Fable coordena como Gerente, subagentes executam o braçal", `4ff85fc`). graphify instalado no sasi (1228 nós/1714 arestas, `466a9ae`); auditoria anti-alucinação 24/24 arestas corretas, economia medida ~64x menos tokens por consulta.
- 2026-07-03: auditoria da frota — `fiscal` criado, `pubmed-evidence-checker` consertado (nunca tivera as tools MCP), `agents/README.md` novo (`3c3044a`). Resposta à pergunta "dá pra ter Opus líder de squad?": subagente não lança subagente; o papel é da ferramenta Workflow.
- 2026-07-03: Edge Function `health` criada (`206393d`) — no repo, sem deploy nem consumidor. Deno instalado em `~/.deno`.
- 2026-07-04: análise dos 13 zips de skills iniciada (interrompida por ele); `book-to-skill` destacado.
- 2026-07-04/05: **1º teste de fogo real** do fluxo ingest/pendências/alertas em plantão UTI2 noturno — 7 leitos reconstruídos, 133+ eventos, alertas vivos. Causa de alerta silencioso achada: evento sem `confidence` vira `requires_review` e não dispara. `VITAL_ALIASES` no frontend (`dde5ca3`+`1a81184`).
- 2026-07-06: faxina geral — MCP `jetbrains` religado; branch web absorvida no repo claude (`6a4c142`); `.gitignore` do sasi reparado (`5e4b481`); achado 🔴 adapter errado na main; adapter mesclado (`017a79c`). 3 zips analisados: `book-to-skill` vendorado (`03f5709`), `mcp-steroid` guardado, `ring-ui` NÃO integrado (conflito com Tailwind).
- 2026-07-06: regra "conferência obrigatória de achado que dispara ação" gravada (`033e365`); `faxina_dev.py` criado. **Bronca de custo**: não executar/verificar braçal pessoalmente, sempre delegar pro modelo mais barato.
- 2026-07-06: reforma do estilo das evoluções — achado 0/66 itens gravados sem acento + vícios de seta/CAPS/didática; skill ganhou `references/00-estilo-texto-clinico.md` (padrão-ouro = texto dele próprio, `c20b353`). Allowlist de 25 entradas REMOVIDA por ordem dele.
- 2026-07-06: graphify ligado no repo `claude` (2040 nós); `fix_ide_mcp.py` reescrito v2; automação por evento via systemd `fix-ide-mcp.path` (zero polling), provada disparando. **Regra permanente: nada de automação por relógio, só por evento.**
- 2026-07-07: mcp-steroid — build verde (JDK 25 em `~/.local/jdk-25`), CLI `devrig`, handshake OK; entrada MCP preparada mas não aplicada.
- 2026-07-10: IDEs sufocando por RAM — faxina de plugins + purga TOTAL de backups (~168MB, ordem dele) + `pc_higiene.py`; ~1,6GB liberados. mcp-steroid fechado ponta a ponta. **Incidente**: reinício da IDE disparado antes de terminar os downloads — lição: esperar a condição da ordem anterior.
- 2026-07-10: faxina estrutural do sasi — docs clínicas migradas pro celebro (`8035e5d`→`440576b`); **o fiscal pegou 2 arquivos que o residente ia deletar por engano** (tinham conteúdo mais novo). Repos `claude` e `jarvis` auditados (`0e987ac`, `5dd90b3`).
- 2026-07-11: skill `admissao-uti` unificada, aprovada por ele. **Bronca nº3 de custo**: leitura longa deveria ter ido pro batedor, não pro agente principal.
- 2026-07-11: **bronca "faxina de merda"** revelou que o lixo estava em `~/dev/_arquivo/`, não no repo. `sasi-import` virou repo GitHub privado; bundles arquivados no OneDrive (rclone check 0 diferenças); gaveta `_arquivo/` dissolvida. `ring-ui-playground` (157MB) deletado; varredura de 5 batedores liberou ~8GB (Junie 668MB deletado por inteiro).
- 2026-07-11: tela de SOFA — pipeline completo batedor→gerente→residente→fiscal ligou a ficha à view; o fiscal achou 1 furo real (mensagem enganosa sem SOFA anterior), corrigido + 2 testes; `c90b6bc..8f9393c`.
- 2026-07-11: confirmado que o Obsidian grava com o app fechado (file-based); graphify estava desatualizado por trava anti-encolhimento silenciosa — `--force` rodado; mcp-steroid reinstalado e provado.
- 2026-07-12: conselho externo sobre IA em UTI (Pydantic/Zod, FHIR) — resposta: o SASI já cumpre validação estrita; FHIR não se aplica hoje (sem feed de prontuário hospitalar), reavaliar no futuro.
- 2026-07-12: migração Netlify→Vercel concluída ponta a ponta; produção final `sasi-uti.vercel.app`.
- 2026-07-16: cofre `.env` reescrito em 12 seções; contas novas assimiladas (AgentMail, Superhuman Docs, Slack, Proton sem dados); troca de conta Claude — config local intacta.
- 2026-07-18: `comando.md` secado 162.109→64.601 chars (−60%); fiscal aprovou, zero débito perdido. Catálogo das 359 skills concluído (workflow, 37 agentes): 90 assimiladas, 9 removidas, 257 descartadas; arsenal 33→123 dirs.
- 2026-07-19: zips-fonte apagados (~223MB); tunagem do WebStorm (SoftRef=50, 8 plugins off, 10 excludes). **Settings Sync mexe em plugin sozinho** — editar config sempre com IDE fechada.
- 2026-07-22: **DataGrip 2026.2 instalada e tunada** (tarball user-local em `~/.local/share/JetBrains/apps/datagrip`, build 262.8665.272): teto `-Xmx1489m`, fonte 16pt/1.4, separadores ON, plugins de IA desabilitados, 3 fontes de dados pré-cabeadas (Supabase pooler + `sasi_index.db` + `dev_index.db`) num projeto próprio `~/.local/share/JetBrains/DataGripProject` pra não brigar com o `.idea` do WebStorm. Não aberta ainda (RAM).
- 2026-07-22: **bronca de custo, a maior até aqui** — ~107k tokens numa instalação trivial. Causas medidas: reconhecimento desnecessário no início, afirmação de licença sem fonte (ele TEM a licença non-commercial e ela cobre DataGrip), skill pesada carregada por conta própria, graphify nunca usado em 19 dias. Diagnóstico do graphify: o hook devolve `additionalContext` — aviso que chega DEPOIS do comando rodar, não é portão; e o padrão `*grep*` casa com `pgrep`, gerando falso "MANDATORY" logo no 1º comando (fadiga de alarme); `~/dev/graphify-out/graph.json` congelado em 06-jul porque `~/dev` não é repo git e o hook pós-commit nunca dispara lá.
- 2026-07-22: contexto fixo medido = ~110.000 chars ≈ 31.000 tokens re-enviados a cada mensagem (comando.md 65.002 · descrições de 118 skills 30.389 · `~/dev/CLAUDE.md` 4.984 · MEMORY.md 4.789 · global 2.492). DÉBITOS+LOG = 58% do comando.md e são registro, não instrução. Achado: `webstorm` e `jetbrains` no `.mcp.json` são o MESMO servidor registrado 2x — 40 tools carregadas em dobro.
- 2026-07-22: **CLAUDE.md global reescrito** — base no arquivo dele da era Gemini (Goggins + Conselho dos Alfas) fundido com as instruções operacionais (PRD antes de construir, pushback, reversibilidade). 92 linhas. 3 contradições resolvidas: Goggins × "explique como pra médico" (vocabulário ≠ postura), "dá todas as sugestões" × "opinião que eu não pedi" (opinião obrigatória sobre o que ele trouxe, proibida como gatilho de ação), Conselho dos Alfas (ângulos de raciocínio, proibido citar nome). Descartado: ponteiro morto pro `PRD-mission-control.md`, que não existe no PC.
- 2026-07-22: `comando.md` cortado 65.002→~14.000 chars. DÉBITOS e LOG saíram pra `debitos.md` e `log.md`, que NÃO carregam sozinhos; no comando.md ficou um índice de 1 linha por débito aberto, pra nada ser enterrado.

## 22-jul-2026 (noite) — Migração ~/dev → ~/projetos
- Job `/file-organizer` renomeou `~/dev` → `~/projetos` (e `_lab` → `rascunhos`) sem reapontar nada; jobs em background morriam com "working directory no longer exists".
- Operador decidiu ADOTAR `~/projetos` (opção 2). Reapontados: symlinks `~/.claude/agents|skills` + `~/projetos/.env` · systemd `fix-ide-mcp.service` · `.mcp.json` · `~/.claude.json` (8 refs) · 33 arquivos (scripts, repo claude, memórias, CLAUDE.md global e do workspace).
- Verificado: systemd exit 0 · query_claude_index OK · graphify OK · push repo claude na main.
- Jobs antigos ancorados em `/home/dr/dev` continuam mortos — relançar do `~/projetos` se ainda fizerem falta.
- Retomada do file-organizer (22-jul noite, pós-migração): segredo Proton → `~/.local/secrets/` (600) · 5 lotes de plantão 18–19/jul DELETADOS por ordem dele (doutrina PHI não se aplica a temporário processado) · 4 docs → OneDrive via arquivar.py (catálogo regenerado: 798 docs) · mensagem-escalistas → celebro/raw · memória de projeto migrada pra `-home-dr-projetos` · deletados npm cache 1,3GB + deb 83MB + graphify-out órfão da raiz + 3 .bak · sasi pushado · lote 4 (rascunhos dev em Downloads) mantido intocado por ordem dele. Bug consertado: regen_index_v2.py apontava pra casa pré-consolidação.

## 23-jul-2026 — Faxina pós-migração
- Achado: job morto de 22-jul tinha despejado `_lab` (14M, 112 arq) e `templates` DENTRO do celebro, aninhados e sem commit. Devolvidos: conteúdo → `~/projetos/rascunhos/` e `~/projetos/templates/`; 2 fichas de paciente (ficha-luiz-*.png) → `celebro/90-PHI-LOCAL/` (fora do git).
- `jarvis` tinha sumido do disco (sem autor rastreável nos transcripts) — reclonado do GitHub em `~/projetos/jarvis`.
- Consertados os 2 scripts que ainda apontavam pro `~/dev` por código (o sed de 22-jul só pegou texto literal): `faxina_dev.py` (DEV, RAIZ_ESPERADA, LIXO_EXCLUIR) e `saude_pc.py:284`. Re-rodados: exit 0, sem alerta estrutural.
- Celebro commitado+pushado (9bd1a3d). 5 `__pycache__` deletados (244K). pc_higiene: 0 a limpar; apt/flatpak pulados (sudo pede senha = parede).
- Sem ação (por ordem/normal): 4 Downloads velhos = lote intocável; portas MCP mortas com 0 IDE aberta; 3 zips em `.idea/dataSources` são cache do DataGrip, não lixo.

## 23-jul-2026 (job segundo plano) — Currículo + Escalistas
- Currículo 2026 finalizado (1 página, verificado visualmente): `Curriculo-Nicholas-Teixeira-Guilherme-2026.docx`+`.pdf` na RAIZ do OneDrive (subidos 17:24). E-mail no currículo agora é SÓ o Gmail `ntg.trabalho@gmail.com` (hotmail removido). Correções da sessão: FAB "(Reserva)" e não "(R2)"; ACLS listado como "ACLS (2023)" sem alegar vigência; BP-SCS descrita como "estágio formativo em TI (ago/2025–mar/2026) + segue como corpo clínico", NUNCA "residente" em documento externo.
- `Mensagem-Escalistas-WhatsApp.docx` criada e subida na RAIZ do OneDrive (17:24): template com placeholders `[NOME]`/`[SERVIÇO/HOSPITAL]` + campo obrigatório de Disponibilidade destacado + checklist "antes de enviar".
- `/meeting` rodada completa (5 personas nativas: life-boss, business-pusher, life-rookie, business-watcher, life-cynic; 3 rounds + síntese). Transcript: `/home/dr/meeting-ajuste-curriculo-e-texto.md`. Decisões: versão única do texto (não 2 versões UTI/PA); disponibilidade específica obrigatória antes de enviar; ACLS fora do WhatsApp; PS da Vila Nhocuné em menção genérica até confirmar o nome; lote inicial de 5 a 8 escalistas na semana.
- Personas do plugin meeting-bots vendorizadas em `~/projetos/claude/agents/` (25 arquivos, `e281202`, pushado) e registradas como `subagent_type` nativos após reinício de sessão — funcionou.
- Endereço Proton do operador LOCALIZADO: `dr.nicholasguilherme@protonmail.com` (kit de recuperação em `~/.local/secrets/proton-recovery-phrase.pdf`). NÃO copiar a frase de recuperação pra lugar nenhum, só o endereço.
- Dra. Nathalia Kokubo é amiga de faculdade do operador (FAMERP).
- Addendum (2ª rodada): e-mail do currículo → Proton `dr.nicholasguilherme@protonmail.com` (conta em uso, correção dele). Disponibilidade preenchida no template e no Objetivo SEM a palavra "imediata" (soava desespero, correção dele). Bullet do PS da Vila Nhocuné removido do currículo. Se escalista perguntar vínculo BP: PJ. Versões finais no OneDrive.

## 23-jul-2026 (sessão 2d0d36e7) — Configuração do cérebro Obsidian
- Grafana Alloy desligado e desabilitado — 272MB RAM liberados.
- Claude Desktop configurado: MCP `basic-memory` (grava em `~/vaults/celebro/basic-memory/`) + MCP `grafana` (binário da extensão + service account); Exa MCP aguardando API key (débito #31, já registrado).
- Cofre `.env` invertido: real em `~/projetos/.env`, `sasi/.env` virou symlink.
- Obsidian (flatpak) virou cérebro operacional: terminal "bash (Tijolão)" via `flatpak-spawn --host` + plugin agent-client (Claude Code via ACP) provados.
- Auditoria dos 33 plugins do celebro via Workflow (33 fichas lidas + síntese com veredito por duelo).
- Faxina de plugins: 23→13 ligados; 16 pastas removidas (`3d-graph`, `mcp-rest`, `hermes-agent`, `livesync`, `file-color`, `icon-folder`, `minimal-settings`, `linter`, `buttons`, `calendar`, `tasks`, `code-linker`, `templater`, `periodic-notes`, `dataview`, `neural-composer`); `calendar`+`tasks` chegaram a ser religados "por mérito" e foram desligados de novo após correção do operador.
- `dataview` substituído por `catalogo.base` (Bases nativo, 3 views) linkado no `index.md`.
- Estrutura PARA fantasma enterrada: `🏠 Home.md` reescrita com o mapa real do vault; pasta `50-Diario` (`_arquivo/stubs-para/`) removida; core Daily Notes desligado.
- Commits no celebro: `ed442b1`→`2835866`→`2a21939`, todos pushados pra `doutortenente/celebro` main. REST `127.0.0.1:27123` provada (HTTP 200) após cada relançamento.

## 28-jul-2026 (sessão faxina + Alpha Council + Goggins, virou 29-jul)
- Faxina de disco no Tijolão: ~16G liberados (84%→69%) — vm_bundles do Claude Desktop (11,3G), Gemini Nano do Chrome (4,1G), 4 `node_modules`, `~/.bun`. Mantidos por ordem: `.cache` e `.npm`. Verificado: `~/.bun` e `vm_bundles` ausentes do disco.
- Workflow manole-onedrive: 59/59 PDFs Manole extraídos de 7 zips, subidos pra `onedrive:Documentos-claude-e-importacao-drive/Estudo/Slides-Manole-UTI/` em 7 módulos, índice regenerado (`_index.json` = 59 entradas), verificação adversarial PASS, zips + staging apagados do Downloads (confirmado: nenhum zip/staging Manole em `~/Downloads`).
- Portão de custo morto: `ULTRACOST_GATE=off` gravado no env de `~/.claude/settings.json` (confirmado por grep; pega no restart). Ordem: plano Max, nunca mais mostrar estimativa/aprovação de custo.
- Sistema de organização SELADO: **ALPHA COUNCIL — WAR ROOM** — Gabinete no topo + S1 Pessoal/S2 Inteligência/S3 Operações/S4 Logística + subáreas (máx 2 níveis) + fila de ação (Agora·Próximo·Aguardando·Arquivar·Descartar). Doutrina em `~/.claude/memory/alpha-council.md` (confirmado, 2763 chars); ponteiros já no `comando.md` (arquitetura + tabela de irmãos). Nome "estudo" VETADO pelo operador — a função é S2 Inteligência.
- `~/.claude/CLAUDE.md` §2 promovido a "DIRETRIZ MÁXIMA SUPREMA — PERFIL GOGGINS" (personalidade/padrões/vieses cognitivos de Goggins em todo Claude; limites: regra 1 tradução + regra 5 zero alucinação).
- PRD pronto: `~/projetos/PRD-alpha-council-war-room.md` (confirmado, 18.995 chars) — War Room no **Cowork ONLINE** (claude.ai), plugin Produtividade. 1ª onda: fundação (fila em TASKS.md + `/despachar`) + domínios `inteligencia/` (fila de digestão dos 59 Manole, skill `/digerir`) e `carreira/` (currículo-mestre + radar de prazos, skill `/curriculo`). Sem relógio, sem janela de horas. Proibição de PHI no workspace removida por ordem dele (fica só a regra de endereço: casa externa entra por ponteiro).
- Claude Desktop/Cowork EXPURGADOS do Tijolão por ordem ("queima tudo"): `~/.config/Claude` (2,0G, 35.357 arquivos) + `~/.cache/claude` apagados, pacote `claude-desktop` purgado via dpkg+sudo. Zero rastro confirmado (dirs e pacote ausentes); Claude Code intocado. Memória auto `claude-desktop-tijolao.md` reescrita. Achado técnico: `SUDO_TIJOLAO_PASSWORD` no `.env` §12 vem entre aspas — limpar antes de usar no `sudo -S`.

## 29-jul-2026 (madrugada ~00:50–02:35 — saneamento MCP/IDE + tokens GitHub)
- **MCP saneado**: entrada órfã `basic-memory` removida do `~/projetos/.mcp.json` (binário inexistente, ENOENT); `jetbrains-steroid` consertado — `JAVA_HOME` apontava pro `~/.local/jdk-25` apagado por faxina, restaurado com Temurin JRE 25.0.3 LTS standalone (62MB, sem vínculo com IDE). Débito 36 e comando.md já refletiam.
- **`scripts/pc/fix_ide_mcp.py` curado de 2 caminhos `~/dev` cabeados** (linha 56 `PROJECT_CFG` + header `IJ_MCP_SERVER_PROJECT_PATH`): eram a fonte do projeto fantasma `/home/dr/dev` no lifecycle da IDE e das entradas mortas `datagrip`/`webstorm` no `.mcp.json`. Provado: `.mcp.json` final só supabase/sasi/obsidian/jetbrains-steroid.
- **Pasta `.claude` do workspace limpa**: hook de proveniência morto (`hooks/post-tool-call.py`, quebrava a cada Write/Edit desde 19-jul) deletado + registro removido; `settings.local.json` reescrito 4.835→1.161 chars (~18 permissões one-shot mortas fora); skill `ide-index-mcp` movida pra `claude/skills/` (casa única) com entrada no `VENDOR.md` — commits `ac028eb`+`dfcd5bb` pushados na main do repo claude.
- **Pasta `.idea` limpa**: 49 arquivos/1,1MB de artefatos velhos do steroid pra Lixeira; módulo renomeado `dev`→`projetos` (`projetos.iml`+`modules.xml`+`celebro.iml`) com IDE fechada, provado na IDE viva ("módulos: celebro, projetos").
- **Settings Sync REINCIDIU**: no reinício da IDE de 01:14 o plugin MCP Steroid entrou sozinho na `disabled_plugins.txt` e o `com.intellij.settingsSync` NÃO estava mais desabilitado (desligamento em dobro de 19-jul não sobreviveu, suspeita de update da IDE). Steroid reabilitado com IDE fechada e provado (porta em 3s).
- **GitHub renovado**: operador revogou os tokens antigos e criou 2 novos, salvos no `.env` §3 (`GITHUB_FINE_GRAINED_PAT`+`GITHUB_CLASSIC_PAT`, "renovado 29-jul-2026"); `gh` autenticado com o fine-grained (enxerga Claude e SASI); `~/.git-credentials` estava MORTA (token revogado) → trocada pro classic novo e provada com `git ls-remote`. Classic novo tem escopo total INCLUINDO `workflow`.
- **TypeScript Language Server no LSP4IJ** (ordem dele): `typescript-language-server` 5.3.0 + `typescript`@5.9.3 em `~/.lsp4ij/lsp/...` (o 7.0.2 não tem `tsserver.js`, rebaixado); registrado via steroid (mappings TS/TSX/JS, `tsserver.path`, node 24 absoluto), status "started". Ressalva DITA ao operador: redundante com o TS nativo do WebStorm, consome RAM extra.
- Sync repos: claude/celebro/sasi em sincronia no nível commitado (nada a puxar/empurrar). Anomalia flagada: `~/projetos/claude/skills/skills/humanizer/` untracked — cópia duplicada da skill já existente em `skills/humanizer/`; não commitada (enterraria o aninhamento errado) nem apagada (aguarda decisão dele).

## 29-jul-2026 (faxina do cofre `~/projetos/.env`)
- `.env` reescrito/enxugado: comentários-novela cortados, moldura de seções 1–15 padronizada (numeração preservada, `§15 Vaultwarden` intacto). Nenhum VALOR de credencial alterado. Verificado no disco: 90 chaves ativas, permissão 600, `sasi/.env → ../.env` intacto, `source` do arquivo sem erro de sintaxe.
- Bug corrigido: bloco MongoDB Atlas (§12) tinha aspas duplas dentro de aspas simples em `MONGODB_USERNAME`/`PASSWORD`/`HOST`/`URI` (valor sairia com `"` literal, quebraria qualquer consumidor); aspas removidas, verificado limpo. Linha morta `MONGODB_ATLAS_API_KEY_OLD` (401, já OBSOLETA) removida.
- Chave RSA do Obsidian (multilinha) tirada de dentro do `.env` e movida pra `~/.local/secrets/obsidian-rest-rsa.pem` (600, verificado); cofre ficou só com o ponteiro `OBSIDIAN_RSA_PRIVATE_KEY_FILE`. Verificado por grep em sasi/claude/celebro/scripts: nenhum consumidor lê a variável antiga diretamente — migração sem risco de quebra silenciosa.
- Apps Script QUEIMADO por ordem do operador: `APPS_SCRIPT_DEPLOY_ID_1`/`_2` removidos em definitivo (ele já tinha deletado várias vezes e alguma sessão ressuscitava); memória permanente `apps-script-queimado.md` já gravada na sessão de origem — conferido aqui: sem sobrevivente em `.env`, `comando.md` ou `debitos.md`.
- `comando.md` corrigido: "13 seções temáticas" → 15 (estava desatualizado; contagem real confere com a moldura travada).
- Sync dos 3 repos (ordem permanente 05-jul): `sasi` limpo, em dia com o GitHub. `claude` e `celebro` têm deleções de arquivo NÃO commitadas de sessão(ões) anteriores (2 arquivos no claude, 13 no celebro — incluindo `Trampo/busca-vagas.xlsx`, `raw/mensagem-escalistas-uti.md`, notas de sessão) — reportadas ao operador em vez de commitadas, por instrução explícita dele nesta tarefa de aguardar OK antes de tocar em pendência fora do `.env`.

## 01-ago-2026 (sessão secretaria — Grande Sincronização e Reestruturação)
- **SASI (b0c79af)**: 126 arquivos commitados e pushados. Inclui a migração completa do schema v3, reatação do SQL/migrations (indentação 2 espaços), atualização dos tipos TS e remoção de documentos obsoletos. Trava de deploy respeitada: build e typecheck (Node 24) passaram com sucesso no sasi-v2.
- **CLAUDE (d35f820)**: 1416 modificações commitadas e pushadas. Reestruturação total da árvore de skills (de plana para categorizada em skills-que-prestam/, pacotao-macaroca-de-skills/ e mapa-claude-e-catalogo-skills/).
- **CELEBRO (9741129)**: 91 arquivos commitados e pushados. Reorganização completa nas 4 camadas Alpha Council (S1 PHI-LOCAL fora do git; S2 Temas-medicina/pesquisas; S3 rascunhos-projetos/arquivos-sasi; S4 Templates). FATOS-CRITICOS.md removido (migrado para o PRD).
- **Saúde do PC**: Boletim 7/8 verde. RAM em 80% (alerta esperado). Conectores MCP jetbrains (porta 64542) e jetbrains-index (porta 29172) respondendo. .env saneado e symlinks do SASI verificados.
- **MCP Steroid**: Configuração confirmada e ativa pro Claude Code, usando JRE 25 standalone.

## 11-ago-2026 (sessão P0 — publicação, caminhos mortos, revisão de CLAUDE.md)
- **P0 10-ago publicado**: PR #7 squash-mergeado na main do `SASI-V3-SEM_MIGUE-` (`b82fa32`, 11 arquivos, +1332/−12 — 8 migrations + `src/types/supabase.ts` + `.claude/rules/supabase.md` + CLAUDE.md retitulado "SASI v3"); main do repo `claude` avançada (`d1c8b2a`, skill `sasi-ingest-export` com o contrato P0 + referência `08-p0-banco-10ago.md`); avisos 1 e 2 da tabela `avisos_agentes` marcados `resolvido=true` no Supabase.
- **6 caminhos mortos de `~/projetos/sasi` consertados** (a pasta v2 não existe mais): 3 scripts de índice reapontados, `saude_pc.py`, `mcp_sasi_wrapper.sh` desativado (exit 1) e entrada `sasi` removida do `~/projetos/.mcp.json`. `comando.md` atualizado em 8 trechos.
- **CLAUDE.md dos 4 repos revisados** por workflow de 8 agentes: criado no `prettify-pal` (93 linhas), editados em `SASI-V3-SEM_MIGUE`, `claude` e `celebro`.
- 🔴 **ACHADO CRÍTICO**: repo `celebro` está **PÚBLICO** no GitHub com **7 arquivos de PHI versionados** — a pasta real chama `PHI-LOCAL/` e o `.gitignore` protege `90-PHI-LOCAL/`, então o filtro nunca pegou. Ação é do OPERADOR: tornar privado + `git rm --cached`. Achado secundário: `pnpm check` do SASI **falha** com 5 erros de tipo em `SystemPanel.tsx`.
- Chave `GITHUB_PAT_P0` gravada no cofre `~/projetos/.env`; operador decidiu **NÃO rotacionar**. `defaultMode=bypassPermissions` ativado na settings global por ordem dele.
- **Sync de repos**: `claude` fast-forward 57ec4b5→`d1c8b2a` (2 arquivos sujos preservados). `SASI-V3-SEM_MIGUE` **NÃO sincronizado** — divergiu (5 commits locais × 1 remoto, sem fast-forward) e `CLAUDE.md` está sujo local (112+/45−) e também alterado pelo remoto (28+/1−). Nada foi stashado, resetado nem descartado.
